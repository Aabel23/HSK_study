# Runtime dependencies for Chinese Study.
# Mirrors [project].dependencies in pyproject.toml.
#
#   pip install -r requirements.txt

fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6
edge-tts>=7.0

# Only required by the Render deploy, which runs gunicorn+uvicorn worker
# (see gunicorn.conf.py / hsk_study/wsgi.py). Not used by the desktop app.
gunicorn>=21; sys_platform != "win32"
uvicorn-worker>=0.2; sys_platform != "win32"
