# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Node / web frontend ----------------------------------------------------
# frontend-web/dist/ IS committed: Render's deploy environment has no Node,
# so the built SPA ships as tracked static files. Rebuild + commit after
# changing frontend-web/src.
node_modules/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Build artefacts (PyInstaller) ------------------------------------------
/build/
/dist/
*.exe
*.msi

# --- Local user data --------------------------------------------------------
# The SQLite database and the generated audio cache are per-machine state,
# not source. The folder itself is kept via data/.gitkeep.
/data/*
!/data/.gitkeep

# --- Environment & logs -----------------------------------------------------
.env
.env.*
!.env.example
*.log
logs/

# --- Vendored helper skill (not part of the application) --------------------
ui-ux-pro-max-skill/

# --- Source dictionary downloaded by scripts/translate_meanings.py ----------
# 10 MB, only needed to rebuild scripts/data/; the rebuilt JSON is committed.
scripts/_cvdict_cache/
scripts/_character_cache/

# --- Standalone PayOS demo kept for reference -------------------------------
# Not part of the application, and it holds live API keys in plain text.
# The real integration lives in backend/services/donate_service.py.
payos/

# --- OS / editors -----------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
